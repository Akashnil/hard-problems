## frankl
Spec review: required
Target to beat: 0.38271 = current record (Liu, arXiv:2306.08824). **No bound is moved this round** (upper or lower). This is a CORRECTION + CLARIFICATION round: remove a now-false numerical claim from the round-2 certificate, record the (L)≠(L') separation as a new clarifying result, and document the eroding-margin red flag on (L). The unconditional integer-t core and the obstruction verdict are invariants, preserved untouched.

---

### Pre-flight (outliner-confirmed, not to be trusted blindly by the builder)

I reproduced the explorer's headline finding from scratch before writing this outline. Two things the builder MUST internalize:

1. **The finding is REAL.** (L') is false on a genuine union-closed family: `|F|=52`, `cond(Z)=129.5`, `min_x B_l(x) = -0.0112203045809301092` at `l=0.126`, verified at 60-digit mpmath. (L) holds on the SAME family (`min P_{1+s} = +3.7e-4`). Onset is exactly `n=8` (zero negatives for `n<=7` in a 400-seed-per-n sweep; 11/392 at n=8, 21/392 at n=9), which is precisely why the round-2 sweep (n=3..7) missed it.

2. **The explorer's RNG attribution is WRONG and the build step must not copy it.** The explorer says `rand_family(9, seed=1711)` "in the round-2 certificate's RNG convention" gives `|F|=52`. It does NOT. The round-2 `rand_family` uses `k = rng.randint(2,7)`; with that convention `rand_family(9,1711)` gives `|F|=21` and `min B_l = +0.0011 > 0` (NO counterexample). The witness with `|F|=52` requires `k = rng.randint(2,12)` (wider generator count). The builder must therefore EXHIBIT and SAVE the explicit witness family (as a frozen list of bitmasks, not a fragile RNG recipe), and re-derive `min B_l < 0` from that frozen list — never from "seed 1711 in the round-2 convention," which does not produce it. This is the single most likely place the build step silently fails to reproduce. (Working witness: `random.Random(1711)`, `k=randint(2,12)`, then `k` draws of `randint(1, 2^9-1)`, OR-closed — but FREEZE the resulting masks.)

---

Angle 1 (top pick): Correct the certificate and record the (L)≠(L') separation as a clarifying result
  Moves: no bound. Aim: replace the false R2-7 "B_l >= 0 on the sweep" assertion with (i) an explicit, frozen, high-precision counterexample to (L'), and (ii) the true lemma (L) as the load-bearing numerical claim, swept past the round-2 cutoff (n>=10), with the eroding-margin caveat recorded.
  Skeleton:
    1. **Freeze the witness.** In the round-3 certificate, hard-code the witness family `F*` as an explicit sorted list of integer bitmasks (n=9, |F|=52), independent of any RNG. Assert `or_closed(F*)` and `len(F*)==52` — by the round-1 `or_closed` helper. This makes the counterexample reproducible forever and immune to RNG-convention drift.
    2. **Reproduce min B_l < 0 from scratch at high precision.** Compute `B_l(F*, l) = Z^{-1}(Q^2/(Q+l))` over a refined l-grid near 0.126; assert `min_x B_l < -1e-3` (float64), then re-confirm the worst (x,l) with an independent mpmath LU-solve at >=50 dps, asserting `B_l(x*) < -0.011` — by direct linear solve, cross-checked float64 vs mpmath. Also record `cond(Z*) ~ 129` to document it is not an ill-conditioning artifact.
    3. **Confirm (L) still holds on the SAME family.** Assert `min_{s in (0,1)} min_x P_{1+s}(F*, x) > 0` (observed +3.7e-4) on a dense s-grid — by direct `Z^{-1} Q^{1+s}`. This is the explicit (L)≠(L') separation: one family on which the surrogate fails but the target survives. Add a one-line numeric explanation: the negative B_l mass sits at small l where the weight `l^{s-1}` (s near 1) downweights it, so `P_{1+s} = (sin(pi s)/pi) int_0^inf B_l l^{s-1} dl` stays nonnegative — and verify the Pick representation still reproduces this P_{1+s} from this B_l numerically (reuse R2-7(i)).
    4. **Relabel R2-7(ii).** Replace the certificate check `"REDUCED LEMMA content: B_l(x) >= 0 for all l>0 (sweep)"` (which PASSES only because SAMPLE_FAMILIES caps n at 7) with TWO checks: (a) `"(L') is FALSE in general: explicit witness min B_l = -0.0112 < 0"` (asserts the negativity — this check PASSES by finding the counterexample), and (b) `"(L) holds on the witness and on a sweep to n>=10: min P_{1+s} > 0"` (the true lemma, now the load-bearing numerical claim). The non-vacuity guard already present (wrong Q^1 numerator) stays.
    5. **Document the onset.** Record in the certificate output that (L') has zero failures for n<=7 and first fails at n=8 (prevalence ~3% by n=9 in this RNG family), explaining why round 2 missed it — by the n-by-n sweep I ran (reproduce a small version, e.g. 100 seeds/n for n in 7..9, asserting 0 failures at n=7 and >=1 at n=8).
    6. **Sweep (L) past the cutoff and record the eroding margin honestly.** Sweep (L) to n>=10 and report `min P_{1+s}` as a function of max n (observed: ~1e-3 at n<=7 down to ~6.7e-5 at n=10), pushing s->1^-. Record this as a monotone-decreasing margin and state plainly that (L) is now a conjecture with a NON-TRIVIAL chance of being FALSE at scale (not merely "unproven") — mirroring exactly how (L') looked positive until n=8.
  Hard step: **Step 2 — reproducing min B_l < 0 from scratch on a frozen witness, not via the explorer's (incorrect) RNG recipe.** Mechanism: the explorer's stated recipe (`rand_family(9,1711)` in the round-2 convention) does NOT regenerate the witness (it caps k at 7, giving |F|=21, B_l>0); the witness needs a wider k-range. If the builder copies the explorer's recipe verbatim the counterexample evaporates and the "correction" would be spurious. Freezing the masks and solving directly removes all RNG dependence and makes the negativity a fixed, re-checkable fact. The negativity itself is real and robust (60-digit confirmed, cond(Z)~129), so once the right family is frozen the linear-algebra step is routine.
  Check: builder runs the round-3 certificate; the new (L')-false check PASSES by exhibiting `min B_l < -0.011` (float64) and re-confirms it at >=50 dps via mpmath; the (L) check PASSES with positive but documented-shrinking margin; the onset check PASSES (0 failures n<=7, >=1 at n=8). Cross-check float64 against mpmath agrees to printed precision. Re-run round-1 and round-2 certificates and confirm they still exit 0 EXCEPT the now-corrected R2-7(ii), which is being deliberately edited (see Angle-1 step 4 — the round-2 cert's R2-7(ii) assertion is the one thing this round changes).

Angle 2: Writeup correction in obstruction-round2.md (paired with Angle 1, not standalone)
  Moves: no bound. Records the separation in prose and removes the overclaim.
  Skeleton (briefer):
    1. Edit §6.2/§6.3: keep the Pick REDUCTION (it is a valid sufficient condition and correctly derived), but replace "(L') would FOLLOW... and is certified numerically with zero failures, margin bounded away from zero" with the corrected statement: (L') is a *strictly stronger sufficient* surrogate that is now known FALSE (explicit witness, §new), so (L') CANNOT serve as a route to (L); the implication (L')=>(L) remains valid but is one-directional and the premise is false.
    2. Add a short subsection "(L)≠(L') separation (round 3)" stating the witness, the high-precision negative B_l, that (L) survives on it, and the integral-weighting reason. Frame as a small genuine clarifying advance: it narrows the open conjecture to its true form (L) and rules out the entire class of per-l / per-minor / operator-positivity surrogates (TP, complete-monotonicity-in-l, Lowner, Pick-as-stated) which all imply the false (L').
    3. Edit §7 and §9: in the "(Conjecture, certified-but-unproven)" bullet, split (L) from (L'): mark (L') FALSE (disproven this round), keep (L) conjectural but upgrade the honesty from "unproven" to "unproven AND with eroding-margin evidence it may be false at scale." Leave §3 (integer-t core), §4 (additivity), §5 (single-lemma reduction (L) form) and the §8 verdict UNTOUCHED.
  Hard step: **keeping the round-2 logical chain intact while excising the false node.** Mechanism: the reduction (L)<=(L') was the round-2 selling point; the builder must show that removing (L') does NOT weaken the verified claim, because (L)/(L') was ALWAYS labelled conjectural and explicitly NOT part of the held/verified result (current.md, §6 intro). The verified content is the integer-t core + additivity + single-lemma reduction, none of which touch (L').
  Check: a reviewer reads the diff and confirms (a) no PROVEN/unconditional statement changed, (b) the false (L') claim is gone or correctly relabelled false, (c) the (L)≠(L') separation is stated with the witness, (d) the verdict in §8 is byte-identical in substance.

Angle 3: Add an explicit "invariants preserved" check block to the round-3 certificate
  Moves: no bound. Defensive — guarantees the correction did not disturb the core.
  Skeleton (briefer):
    1. Re-run (import or re-derive) the integer-t identity `Z^{-1}Q^m == law(OR of m iid uniform)` for m=2,3,4 on the witness family and the non-distributive family; assert equality and nonnegativity — by brute-force OR law vs `P_t`. This re-affirms `H[P_m] <= H[A]` (the unconditional core) survives on the very family where (L') fails.
    2. Re-assert additivity `P_{a+b} == P_a (*) P_b` and `M_F` column-stochastic on the witness family — by the round-2 joinconv/M_F helpers.
    3. Print an explicit banner: "UNCONDITIONAL integer-t core UNCHANGED; record 0.38271 (Liu) UNCHANGED; obstruction verdict (approach cannot reach 1/2) UNCHANGED. Only the conjectural non-integer-t surrogate (L') is corrected (now FALSE); (L) remains conjectural with eroding-margin caveat."
  Hard step: none load-bearing — this is verification scaffolding. The only subtlety: ensure the integer-t core check is run on the n=9 witness (where (L') fails), to demonstrate concretely that the core is insulated from the (L') failure.
  Check: certificate exits 0 with the invariants block PASSING on the witness family; the banner text matches the writeup's §9 conclusion.

---

Ranking:
- **Angle 1 is the load-bearing deliverable** and must be done first: until min B_l < 0 is reproduced from scratch on a frozen witness at high precision, nothing else may be rewritten (this is the explicit user instruction — independent confirmation gates the rewrite). The single real risk is the explorer's incorrect RNG recipe; I have already located the correct one (wider k) and the fix is to freeze the masks. Effort: low-moderate, mostly bookkeeping plus one mpmath solve.
- **Angle 2 is mandatory and paired with Angle 1** (the writeup must match the corrected certificate) but cannot precede it — it consumes Angle 1's confirmed witness. Effort: low (surgical edits; the bulk of the round-2 writeup is invariant and must NOT be touched).
- **Angle 3 is cheap insurance**, recommended to make the "core untouched" invariant machine-checked rather than merely asserted in prose, and to demonstrate concretely on the n=9 witness that the unconditional core survives where (L') dies.

Why review is REQUIRED: the deliverable's whole point is to RETRACT a previously reviewer-verified numerical assertion (round-2 R2-7(ii) "B_l >= 0 on the sweep"). A retraction of verified content must be re-reviewed for (a) correctness of the new counterexample (high-precision, frozen, RNG-independent), (b) that the retraction is correctly scoped — it touches ONLY the conjectural surrogate and leaves the unconditional integer-t core, the additivity/reduction, the obstruction verdict, and the record 0.38271 provably untouched, and (c) that the new "(L) may be false at scale" framing is honest and not overstated into a claim that the obstruction is weakened (it is not — the obstruction never depended on (L)). This is exactly the kind of non-routine, integrity-sensitive change that demands outline review.

Note on the explorer's recommendation NOT to attempt an analytic proof of (L): I AGREE, and reproduced enough to endorse it. Every black-box framework (total positivity, complete-monotonicity-in-l, Lowner/operator-monotone, Pick-as-stated) routes through the per-l positivity (L'), which is now an explicit false statement — so none can close (L). A proof would require a novel native combinatorial lemma about s-averaged Mobius sums of convex-monotone valuations on arbitrary join-semilattices, the target may itself be false at scale (eroding margin), and even if proven it moves NO bound and does not change the verified obstruction (which rests on the unconditional integer-t core). ROI against the goal is essentially zero. Do not attempt it this round.
